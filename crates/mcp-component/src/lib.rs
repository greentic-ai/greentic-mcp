#![allow(unused)]

// Placeholder for a future Wasm component shell that exposes the wasix:mcp surface.
// The crate is wired into the workspace so we can evolve the executor and a reference
// component in lockstep once the interface is finalized.
